# Repository Instructions

Before changing code or documentation, read and follow:

- `CLAUDE.md` for repository-wide development rules
- `README.md` for setup and project structure
- `docs/frontend-conventions.md` for detailed frontend conventions
- The document related to the current task under `docs/assignments/`, `docs/rfc/`, or `docs/week-07-performance/`

Use the scripts in `package.json` as the executable source of truth for development and verification commands.

Do not duplicate repository rules in this file. Keep shared guidance in the existing project documents.
