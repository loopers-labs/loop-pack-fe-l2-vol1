# 빌드 산출물·의존성 — 우리가 작성하는 코드가 아니다
dist
node_modules

# 패키지 매니저가 관리 — Prettier가 건드리면 안 된다
pnpm-lock.yaml

# 바이너리/벡터 에셋 — 포맷 대상 아님
*.svg
public

# 도구 상태 디렉토리 — 우리 코드가 아니다
.claude
.omc

# 마크다운은 산문(표·의도적 줄바꿈) — Prettier는 '코드만' 관리한다
*.md
