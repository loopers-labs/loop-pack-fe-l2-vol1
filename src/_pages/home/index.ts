// 홈 슬라이스의 Public API. 라우팅 파일이 이 계약만 보고 화면을 붙인다.
// 슬라이스 내부 파일(HomeContent 등)은 여기서 공개하지 않는다 — 외부에서 조립할 대상이 아니다.
export { HomePage } from '@/_pages/home/ui/HomePage'
export { HomeLoading } from '@/_pages/home/ui/HomeLoading'
