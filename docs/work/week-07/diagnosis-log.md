# 7주차 성능 진단 기록표

> 재현 조건은 [measurement-conditions.md](./measurement-conditions.md), Before 원본 관찰은 [baseline-and-regression.md](./baseline-and-regression.md) 0단계를 따른다. "상세" 열이 가리키는 단계별 변경 근거는 [01-hero-lcp.md](./01-hero-lcp.md)·[02-list-pending-cls.md](./02-list-pending-cls.md) 참고.

---

## 🔍 진단 기록표

| 관찰한 사실                                                                                                                                                                                                                                                   | 원인 가설                                                                                                      | 반증할 측정                                                                                                                                                           | 가장 작은 변경                                                                                                         | 결과 (채택 / 되돌림 / 무개입)                                                                                                                                                                                                                                             | 상세    |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| LCP 중앙값 8,225.4 ms 중 이미지 전송이 8,000.8 ms로 **97.3 %**를 차지한다. Hero 전송량은 7,545,525 B이고 화면 표시 크기는 1200×675인데 이미지 원본은 3840×2160이다                                                                                            | 표시 크기의 약 10배 넓이(3840×2160)인 원본을 리사이즈·재압축 없이 그대로 전송해, 전송 시간이 LCP를 지배한다    | 표시 크기에 맞춰 리사이즈·재압축한 이미지로 교체하고 같은 조건에서 재측정한다. 전송 바이트가 줄었는데도 이미지 전송 구간이 그에 비례해 줄지 않으면 가설이 틀린 것이다 | Hero 이미지를 표시 크기에 맞는 해상도·포맷으로 재인코딩해 전송 바이트만 줄인다 (레이아웃·비율·주요 피사체·문구는 유지) | **채택** — `next/image`(`fill`+`sizes`)로 표시 크기(1200×675)에 맞춰 WebP로 재인코딩. 전송 7,545,525 B→80,965 B(98.93 % 감소), 이미지 전송 구간 8,000.8 ms→319.7 ms, LCP 중앙값 8,225.4 ms→538.2 ms(93.46 % 감소). 전송 바이트 감소에 비례해 전송 구간이 줄어 가설 확인됨 | 1단계   |
| 홈 문서 응답은 쓰로틀링 없이 TTFB 6~12 ms이고, LCP의 서버 응답 대기 구간은 13.9 ms(**0.17 %**)다. filmstrip 첫 프레임(1,025 ms)에서 Header·`h1`·페이지 설명·카테고리가 이미 모두 표시된다                                                                     | "`HomePage` 전체가 홈 데이터를 기다려 waterfall이 생긴다"는 사전 가설                                          | document 응답 시간과 LCP 구간 분해의 서버 응답 대기 값, filmstrip 첫 프레임의 표시 내용                                                                               | 렌더링 경계를 데이터 소유권에 맞게 분리                                                                                | **무개입** — 0단계 시점 실측이 가설을 지지하지 않았다. 당시 `getHome()`은 week-05 direct-call 우회로 SSR에서 지연 없이 즉시 반환해 홈 데이터를 기다리는 비용이 사실상 0이었고, 경계를 나눠도 LCP의 97.3 %인 이미지 전송은 그대로 남는다.<br>**3단계 이후 정정**: `6f016256`으로 그 우회를 제거해 홈도 `/api/home`을 실제 HTTP 호출하게 되면서 서버 응답 대기가 521.9 ms(LCP의 59 %)로 올라갔다 — "비용이 0"이라는 근거는 더 이상 성립하지 않는다. 무개입 결론은 유지하되 근거가 바뀌었다: 이 521.9 ms는 `/api/home`의 고정 지연 500 ms 자체라 경계를 옮겨도 사라지지 않고, 홈 셸에는 목록과 달리 그 사이 조작할 컨트롤이 없어 셸을 먼저 내보내는 이득이 작다. 상세는 [01-hero-lcp.md](./01-hero-lcp.md) "렌더링 경계 선택" 행                                                                           | 1단계   |
| Hero 이미지에 `fetchpriority`가 적용되어 있지 않다(`priorityHinted: false`). 다만 이미 초기 HTML에서 발견 가능하고 `loading=lazy`가 아니며 요청 priority는 `High`다. 이미지 요청 시작까지의 대기는 166.8 ms(**2.0 %**)다                                      | `fetchpriority="high"`로 요청 시점을 앞당기면 LCP가 줄어든다                                                   | 요청 시작 대기 구간의 크기. 이 구간 전체가 166.8 ms이므로 이 변경으로 얻을 수 있는 최대 절감도 그 범위를 넘지 못한다                                                  | `<img>`에 `fetchpriority="high"` 추가                                                                                  | **보류** — 최대 절감 여지가 LCP의 2.0 %에 그쳐, 97.3 %를 차지하는 전송량 문제를 먼저 다룬다                                                                                                                                                                               | 1단계   |
| CLS가 5회 모두 0이고 이동한 요소가 없다. Hero `<img>`에 `width={3840}`·`height={2160}`이 지정되어 있다                                                                                                                                                        | 이미 공간이 예약되어 있어 fallback 교체 시 밀림이 없다                                                         | Layout Shifts 값과 이동 요소 목록                                                                                                                                     | 없음                                                                                                                   | **무개입** — 현재 조건을 이미 만족한다. 이미지 교체 시 이 속성이 유지되는지만 4단계에서 재확인한다                                                                                                                                                                        | 1·2단계 |
| `APP_ORIGIN`을 닿지 않는 origin으로 두고 SSR self-fetch를 강제 실패시켜도 사용자에게 에러가 노출되지 않는다. `prefetchQuery`가 에러를 삼키고 `dehydrate`가 실패한 쿼리를 직렬화하지 않아, 클라이언트가 빈 캐시로 hydrate된 뒤 스스로 재요청해 성공으로 덮인다 | 최초 진입 실패 상태(`ProductResultsError`)가 실제로는 거의 도달 불가능한 코드다                                | 서버 로그에 실패가 기록되는지, 클라이언트가 실제로 재요청하는지 Network에서 확인                                                                                      | SSR prefetch 실패를 클라이언트로 전파하거나(`onError`로 별도 상태 저장), 최소한 서버 로그로 실패를 남긴다              | -                                                                                                                                                                                                                                                                         | 2단계   |
| 정상 목록(30개) 표시 중 갱신 요청만 실패시키면 기존 목록이 사라지고 결과 영역 전체가 에러 UI("mock error" + 다시 시도)로 교체된다. 카테고리 select도 옵션 1개로 붕괴한다                                                                                      | `isError` 분기가 `isPlaceholderData`/기존 데이터 유무를 고려하지 않고 무조건 결과 영역 전체를 에러 UI로 바꾼다 | 갱신 실패를 재현했을 때 이전 `productList`가 화면에 남아있는지                                                                                                        | `isError && !productList`일 때만 전체 에러 UI를 보여주고, 기존 데이터가 있으면 목록은 유지한 채 에러 배너만 얹는다     | **채택** — `findLastSuccessfulProductList`로 query cache에서 직전 성공 응답을 찾아 대체 렌더. 재현 시나리오를 수정 전/후 코드 각각에 실행해 대조: 수정 전은 문제 그대로 재현(카드 0개, 전체 에러), 수정 후는 카드 12개 유지 + "갱신 실패 — 아래는 이전 조건의 결과입니다" 문구로 확인됨 | 2단계   |
| 정렬만 바꿔 건수가 같은 6건→6건 갱신에서도 CLS 0.226이 발생하고, 그 시점 카드 제목 높이가 69~140px로 들쭉날쭉했다 | 상품명이 2줄을 넘어 3줄이 되는 카드가 있어 카드 높이가 제각각이고, 그게 정렬 변경 시 레이아웃을 밀어 CLS를 만든다 | 제목을 2줄로 클램프해 카드 높이를 균일화한 뒤 같은 시나리오로 CLS를 재측정. 균일화됐는데도 CLS가 그대로면 가설이 틀린 것 | `.product-card h2, h3`에 `-webkit-line-clamp: 2`, `min-height`→`height`로 상한 고정 | **반려(가설 기각)** — 제목 높이 56px로 완전 균일화, 본문 높이 불변(1617→1617)됐지만 CLS는 0.217439로 클램프 전(0.217439)과 동일. `LayoutShift.sources`가 가리키는 요소는 카드 높이가 아니라 재정렬로 그리드 내 위치가 이동한 `<article class="product-card">` DOM 노드 자체(예: x=800→68) — key로 재사용되는 노드의 이동량 자체를 클램프는 줄이지 못해 무관한 대응이었음. 부수 확인: slow API의 1.5초 지연이 Layout Instability API의 500ms recent-input 유예를 넘겨, CDP `Input` 도메인의 신뢰된 클릭·키보드 이벤트로도 동일 CLS(0.21743931167879527) 재현됨 — 실제 사용자 조작이었어도 집계됨 | 2단계 |

> 5회 raw 값의 범위보다 작은 변화는 개선으로 주장하지 않는다.
