# 11전략 Bad Code Smell 체크리스트

> 대상: `src/market/` 전체 파일

## 경계

| #   | 전략          | 위치                                             | 왜 냄새인가                                                                                                                                                                                | 고칠 방향                                                                                                                                      | AI 활용 |
| --- | ------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| ①   | 변화의 경계   | `Price.tsx:10` + `CheckoutPage.tsx:252,267`      | `Price` 컴포넌트가 표시 전용인데 VIP 10% 할인 비즈니스 로직을 내부에 숨기고 있음. 같은 `finalPrice`를 사용하는 `Price`(할인 적용)와 버튼(할인 미적용)에서 금액이 다르게 표시되는 경계 침범 | VIP 할인 로직을 `Price`에서 제거하고 `CheckoutPage`의 가격 계산 영역으로 이동. `Price`는 받은 `amount`를 포맷만 하는 순수 표시 컴포넌트로 변경 | X       |
| ②   | 구현 vs 조합  | `DeliveryMemo.tsx` 전체 + `CheckoutPage.tsx:169` | `DeliveryMemo`가 `memo` 상태를 내부에서 소유하고 있어 부모(`CheckoutPage`)에서 값을 읽을 수 없음. 결제 폼의 일부인데 서버 전송 시 memo가 빠지는 구조                                       | 상태를 `CheckoutPage`로 끌어올리고 `DeliveryMemo`는 `value`/`onChange`를 받는 제어 컴포넌트로 변경하여 부모와 조합 가능하게 변경               | X       |
| ③   | God Component | 해당 없음                                        | `CheckoutPage`가 크긴 하지만 렌더링은 서브컴포넌트에 위임했고, 페이지 컴포넌트로서 상태 조율 역할은 정상 범위                                                                              | —                                                                                                                                              | —       |
| ④   | 성급한 추상화 | `_components/Price.tsx` + `CheckoutPage.tsx:162` | ① 리팩토링 후 `Price`가 `<strong>{amount.toLocaleString()}원</strong>` 한 줄짜리 포맷팅만 남음. 별도 파일 + import까지 하지만 사용처 1곳뿐이라 컴포넌트로 분리할 필요 없음                 | `Price` 컴포넌트 제거, `CheckoutPage`에서 인라인으로 교체                                                                                      | O       |

## 계약

| #   | 전략                          | 위치                                                     | 왜 냄새인가                                                                                                                                                                                                                                                                                                       | 고칠 방향                                                                                                                                                                                          | AI 활용 |
| --- | ----------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| ⑤   | props는 적게, 이름은 역할대로 | `OrderLineRow.tsx` 전체 + `CheckoutPage.tsx:174,231-244` | props 8개(`type`, `label`, `amount`, `thumbnail`, `option`, `quantity`, `isDiscount`, `couponCode`) 중 `type`에 따라 실제 쓰이는 조합이 다름. `product`일 때만 `thumbnail`·`option`·`quantity` 사용, `coupon`일 때만 `couponCode`·`isDiscount` 사용 → 호출부에서 어떤 props를 넘겨야 하는지 타입만으로 알 수 없음 | Composition 패턴 적용: `OrderLineRow`는 `children`만 받는 레이아웃 컨테이너로 변경하고, `Thumbnail`·`Label`·`Amount` 서브컴포넌트를 정적 프로퍼티로 분리. 호출부에서 필요한 조각만 조합하도록 변경 | X       |
| ⑥   | boolean 폭발 → enum           | `OrderStatusTag.tsx` 전체 + `CheckoutPage.tsx:280`       | `o.status`를 그대로 넘겨서 `OrderStatusTag` 내부에서 처리하면 되는데, 호출부에서 `status`를 `isPaid` · `isPreparing` · `isShipped` · `isDelivered` · `isCancelled` 5개 boolean으로 분해해서 모두 넘기고 있음                                                                                                      | `status: OrderStatus` 단일 prop으로 변경. 호출부는 `o.status`만 전달하고, label·color 매핑은 컴포넌트 내부 책임으로 이동                                                                           | X       |
| ⑦   | 파생 상태 + key               | `CheckoutPage.tsx:132`                                   | `finalPrice`는 `itemTotal` + `shippingFee` - `couponDiscount` - `pointDiscount`에서 계산만 하면 되는 파생값인데 `useState`에 담고 있어 초기 렌더링 값이 고정됨. 쿠폰·적립금 변경 시 금액이 갱신되지 않는 버그                                                                                                     | `useState` 제거 → `totalDiscount`(쿠폰+적립금)와 `finalPrice`를 일반 `const` 계산으로 교체하여 렌더링마다 재계산                                                                                   | X       |
| ⑧   | 확장은 위임으로               | `OrderLineRow.tsx` 전체 (⑤와 동일 위치)                  | 새 줄 타입(부분취소, 선물포장 등)이 추가될 때마다 컴포넌트 내부 `type` 분기(`if`/`switch`)를 수정해야 하는 구조. 확장이 내부 수정을 강제함                                                                                                                                                                        | ⑤의 Composition 리팩토링으로 동시 해결: 호출부에서 서브컴포넌트를 조합하므로 새 타입 추가 시 컴포넌트 수정 불필요                                                                                  | O       |

## 합성

| #   | 전략                     | 위치                                                                    | 왜 냄새인가                                                                                                                                                                  | 고칠 방향                                                                                                                                                               | AI 활용 |
| --- | ------------------------ | ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| ⑨   | Context 전에 composition | `CheckoutPage.tsx:145-161` → `_components/OrderCompleteStep.tsx`로 분리 | 주문 완료 UI를 별도 컴포넌트(`OrderCompleteStep`)로 추출할 때, 기존 값(`finalPrice`, `memo`)을 공유하기 위해 Context를 도입할 수 있으나 부모→자식 1단계 관계이므로 과잉 설계 | `OrderCompleteStep` 컴포넌트로 분리하고 props(`finalPrice`, `memo`, `onBack`)로 직접 전달. Context는 멀티스텝 플로우에서 형제 컴포넌트 간 공유가 필요해지는 시점에 도입 | X       |
| ⑩   | children vs slot         | `CheckoutPage.tsx:179-187` → `_components/TermsModal.tsx`로 분리        | 모달의 overlay/dismiss 패턴과 약관 콘텐츠가 인라인으로 섞여 있어 모달 구조 재사용 불가. 다른 모달이 추가되면 같은 overlay/dismiss 코드가 반복됨                              | `TermsModal` 컴포넌트로 분리하고 `isOpen`/`onClose`로 제어. 향후 모달이 추가되면 공통 `Modal` 셸 + `children`으로 콘텐츠 주입하는 구조로 확장 가능                      | X       |
| ⑪   | Drilling vs Context      | 해당 없음                                                               | `DeliverySection` → `AddressForm` → `AddressField`로 3단계 전달되지만, 각 중간 컴포넌트가 props를 실제로 사용하고 있어 순수 drilling 아님                                    | —                                                                                                                                                                       | —       |
