# Week 02 — 셀프 리뷰 결과

> 기준: `REFACTORING_SPEC.md` 하단 Checklist

## 판별

| #   | 항목                                                                          | 통과 | 비고                                                                         |
| --- | ----------------------------------------------------------------------------- | ---- | ---------------------------------------------------------------------------- |
| 1   | 모든 파일을 11개의 전략으로 훑고 해당하는 내용 여부를 별도 Table로 작성했는가 | ✅   | `bad-smell-checklist.md`에 11개 전략 전부 기입 완료                          |
| 2   | "해당 없음"으로 판단한 전략도 근거와 함께 적었는가                            | ✅   | ③ God Component, ⑪ Drilling vs Context — 근거 포함하여 "해당 없음" 기입      |
| 3   | 화면을 직접 조작해 동작에서 드러나는 냄새까지 확인했는가                      | ✅   | ⑦ 파생 상태(쿠폰 적용해도 금액 안 바뀜), ① Price vs 버튼 금액 불일치 등 확인 |

## 리팩토링

| #   | 항목                                                             | 통과 | 비고                                                          |
| --- | ---------------------------------------------------------------- | ---- | ------------------------------------------------------------- |
| 1   | 판별한 냄새만 고쳤는가 (냄새 아닌 곳을 건드리지 않았는가)        | ✅   | 체크리스트에 기입된 항목만 수정, handleX 네이밍은 컨벤션 교정 |
| 2   | 동작을 보존했는가 (가려졌던 버그가 드러나 고쳐진 것만 예외)      | ✅   | ⑦ finalPrice 버그 수정은 "가려졌던 버그가 드러남"에 해당      |
| 3   | Context를 반사적으로 도입하지 않았는가, 도입했다면 근거가 있는가 | ✅   | Context 미도입, ⑨에서 props 전달로 해결                       |
| 4   | 새 any·as·eslint-disable로 타입·린트를 침묵시키지 않았는가       | ✅   | 타입 우회 없음, `pnpm build` 통과                             |

## 프로세스

| #   | 항목                                                             | 통과 | 비고                                                  |
| --- | ---------------------------------------------------------------- | ---- | ----------------------------------------------------- |
| 1   | 셀프 리뷰 4단계를 통과했는가                                     | ✅   | 본 문서로 확인                                        |
| 2   | PR에 AI 생성 부분을 표기했는가                                   | ⬜   | PR 생성 시 체크리스트 AI 활용 컬럼 기반으로 표기 예정 |
| 3   | 리팩토링마다 한 줄 근거를 PR에 남겼는가                          | ⬜   | PR 생성 시 코드 내 주석 기반으로 작성 예정            |
| 4   | (선택) CLAUDE.md에 본인 판단으로 규칙을 추가하고 근거를 적었는가 | ✅   | 아래 "추가한 규칙" 참고                               |

## 추가한 규칙

| 규칙                                                                | 근거                                                                                                                                                                                             |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 하나의 상태를 표현하는 여러 boolean props는 enum/union으로 통합한다 | OrderStatusTag에 boolean 5개(isPaid, isPreparing, ...)를 넘기면 호출부에서 동시에 여러 개를 true로 보낼 수 있어 유효하지 않은 조합이 생긴다. enum/union이면 타입 레벨에서 하나만 선택이 강제된다 |
