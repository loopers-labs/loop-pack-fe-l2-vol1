# 5주차 E2E 테스트 결과

## 실행 정보

- 실행 일시: 2026-07-23 14:30 (KST) — 셀프 리뷰 수정 반영 후 재실행
- 대상: `e2e/week-05-state.spec.ts`
- 브라우저: Chromium, WebKit
- 실행 명령:

```bash
pnpm exec playwright test e2e/week-05-state.spec.ts
```

## 최종 결과

| 항목      | 결과   |
| --------- | ------ |
| 시나리오  | 20     |
| 전체      | 40     |
| 성공      | 40     |
| 실패      | 0      |
| Flaky     | 0      |
| Skipped   | 0      |
| 실행 시간 | 32.1초 |

Chromium과 WebKit에서 각각 20개 테스트가 모두 통과했다.

## 확인한 사용자 흐름

- 홈 데이터가 hydration된 후 브라우저에서 `/api/home`을 중복 요청하지 않는지 확인
- 홈의 찜·담기 상태가 상품 목록과 새로고침 후에도 유지되는지 확인
- 찜 해제가 장바구니 상태와 독립적으로 반영되고 새로고침 후에도 유지되는지 확인
- 검색·카테고리·정렬 변경이 URL과 목록에 반영되고 page가 1로 초기화되는지 확인
- 검색 debounce 대기 중 필터와 정렬을 변경해도 모든 조건이 유지되는지 확인
- debounce가 끝난 검색어가 뒤로·앞으로 이동에서 input·URL·결과와 함께 복원되는지 확인
- 페이지네이션 경계와 뒤로·앞으로 이동에서 URL 상태가 복원되는지 확인
- 조건 변경 요청 중 이전 목록을 유지하고 새 응답 후 목록을 교체하는지 확인

## 확인한 오류·경계·복구 흐름

- 검색어 전체 삭제 후 `q`가 제거되고 전체 목록으로 복원되는지 확인
- 존재하지 않는 상품 검색 시 명시적인 빈 상태가 표시되는지 확인
- 1,000자 검색어가 입력과 URL에 보존되고 안전하게 빈 상태로 처리되는지 확인
- 공백과 URL 예약 문자가 포함된 검색어가 하나의 `q` 값으로 보존되는지 확인
- debounce 대기 중 페이지를 떠나면 예약된 검색어 변경이 취소되는지 확인
- 지원하지 않는 카테고리·정렬 값이 기본 조건으로 복구되는지 확인
- 허용 범위를 벗어난 `page`가 제어된 오류 UI로 처리되는지 확인
- 마지막 페이지를 초과했을 때 빈 상태와 함께 앞 페이지로 돌아갈 페이지네이션이 남는지 확인
- 검색 결과가 0건일 때는 페이지네이션을 표시하지 않는지 확인
- 상품 API가 반복 실패했을 때 오류 UI가 표시되고 다시 시도로 복구되는지 확인
- 손상된 persist 저장값이 빈 장바구니·위시리스트로 복구되는지 확인
- 이전 버전의 persist 저장값이 현재 상태로 migration되는지 확인

## 실행 중 발견 및 조치 (최초 실행, 2026-07-23 00:36)

- 최초 전체 실행: 34개 성공, 4개 실패
- 원인: Next.js route announcer와 애플리케이션 오류 UI가 모두 `role="alert"`를 사용해 범용 locator가 두 요소를 선택함
- 조치: 사용자가 실제로 보는 오류 문구로 locator를 한정함
- 수정 테스트 재실행: 4개 성공
- 최종 전체 회귀 실행: 38개 성공

## 셀프 리뷰 수정 후 재실행에서 발견한 회귀 (2026-07-23 14:30)

셀프 리뷰 지적을 반영하면서 테스트를 두 개 조정하고(`pageSize` URL 제거, 페이지 초과 시 페이지네이션 유지) 0건 검색 시나리오를 하나 추가한 뒤 재실행했다.

- 재실행 1차: 35개 성공, 5개 실패 (검색어를 입력해도 `q`가 URL에 반영되지 않음)
- 원인: 필터를 조회 성공 여부와 무관하게 **항상** 렌더링하도록 바꾸면서, 검색 input이 hydration 이전 SSR HTML에 먼저 나타났다. 그 시점의 입력은 React 이벤트 핸들러에 잡히지 않아 debounce 자체가 시작되지 않는다. 기존에는 데이터가 도착한 뒤에야 input이 생겨 이 구간이 없었다.
- 조치: 노출 기준을 "조회 성공"이 아니라 "조회 완료(`!isPending`)"로 바꿨다. 실패 상태에서는 필터가 남아 조건을 바꿔 빠져나올 수 있고, 최초 로딩 중에는 렌더링하지 않아 hydration 이전 입력 유실이 생기지 않는다.
- 재실행 2차: 40개 전부 성공

이 회귀는 정적 검사(lint·typecheck·build)에서 전혀 드러나지 않았고 E2E에서만 잡혔다. 화면 경계를 바꾸는 수정은 정적 검사만으로 끝내면 안 된다는 근거로 남긴다.

---

_이 문서의 실행 결과는 실제 Playwright 실행 출력을 옮긴 것이며, 정리와 문장 작성은 AI(Claude)와 함께 했습니다._
