# Week-07 성능 측정 기록

## 측정 조건 (Before·After 공통 — SHA 제외 전부 동일하게 유지)

| 항목               | 값                                                     |
| ------------------ | ------------------------------------------------------ |
| URL                | `http://localhost:3000/` (cold load)                   |
| 실행               | `pnpm build` → `pnpm start` (production)               |
| viewport           | (기입: 예. 1440×900 데스크톱 / Lighthouse 기본 모바일) |
| CPU throttling     | (기입: 예. 4x slowdown)                                |
| Network throttling | (기입: 예. Slow 4G)                                    |
| 브라우저 버전      | (기입)                                                 |
| Lighthouse 버전    | (기입)                                                 |
| load 조건          | cold load (캐시 비우고 하드 리로드)                    |
| 브라우저 프로필    | 확장 프로그램·캐시·로그인 없는 별도 프로필             |

## Before

- **Before SHA**: `a353c4eb38758fd1475d0d0722aa57e3d538074a`

### Lighthouse 5회 raw 값 (홈 cold load)

| 회차       | FCP | LCP | CLS |
| ---------- | --- | --- | --- |
| 1          |     |     |     |
| 2          |     |     |     |
| 3          |     |     |     |
| 4          |     |     |     |
| 5          |     |     |     |
| **중앙값** |     |     |     |
| **최솟값** |     |     |     |
| **최댓값** |     |     |     |

### DevTools 관찰

- **LCP element**: (기입: Lighthouse가 지목한 요소)
- **filmstrip 표시 순서**: Header → 페이지 제목 → Hero 순서가 실제로 어땠는지 기입
- **Network waterfall**:
  | 요청                | 시작 시점 | 전송 크기 |
  | ------------------- | --------- | --------- |
  | document (`/`)      |           |           |
  | `/api/home`         |           |           |
  | `hero-original.jpg` |           |           |
- **Layout Shifts**: (기입: Layout shifts track에서 관찰한 이동과 원인 요소)

### 목록 slow 녹화 (`/products?scenario=slow`)

- **데이터 없는 최초 진입** (하드 리로드): (기입: 1.5초 동안 무엇이 보이는지, pending UI 유무)
- **기존 목록이 있는 갱신** (검색·카테고리·정렬·페이지 변경): (기입: 목록이 비워지는지, 갱신 중 표시 유무)
- **연속 조건 변경**: 현재 URL의 active query와 화면 결과 일치 여부, 이전 요청의 늦은 완료가 현재 화면을 덮는지 기입
- **취소된 요청**: (기입: Network에서 canceled 요청이 어떻게 보이는지, 오류로 노출되는지)

### 판단 (각 한 문장)

- **관찰한 사실**:
- **원인 가설**:
- **가설을 반증할 방법**:
- **먼저 시도할 가장 작은 변경**:

## After

- **After SHA**: (4단계에서 기입)

### Lighthouse 5회 raw 값 (Before와 같은 조건)

| 회차       | FCP | LCP | CLS |
| ---------- | --- | --- | --- |
| 1          |     |     |     |
| 2          |     |     |     |
| 3          |     |     |     |
| 4          |     |     |     |
| 5          |     |     |     |
| **중앙값** |     |     |     |
| **최솟값** |     |     |     |
| **최댓값** |     |     |     |

### Before 대비 비교

- **LCP element 변화**:
- **Hero 이미지 전송 크기·요청 시작 순서 변화**:
- **가장 길었던 구간의 변화**:
- **측정 흔들림(5회 범위)보다 큰 변화인가**:
- **효과 없거나 악화된 변경과 유지/되돌림 판단**:

### 회귀 확인

- 목록 최초 진입·갱신 재녹화, URL 복원(검색·카테고리·정렬·페이지):
- 뒤로/앞으로 가기:
- 장바구니·위시리스트·Header 개수:
- 로딩·에러·빈 상태·재시도:
- FSD 의존 방향·슬라이스 Public API 우회 여부:
